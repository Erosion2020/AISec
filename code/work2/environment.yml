name: aisec
channels:
  - pytorch
  - nvidia                # CUDA runtime（cudart, cublas 等）
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pytorch>=2.0
  - pytorch-cuda=12.4     # 强制 CUDA 版本，避免回退到 CPU
  - numpy>=1.21
  - pandas>=1.3
  - tqdm>=4.60
  - pip